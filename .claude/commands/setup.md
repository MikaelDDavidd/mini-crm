---
description: Install dependencies for frontend and backend
allowed-tools: Bash(*)
---

!cd frontend && npm install && cd ../backend && npm install

Dependencies installed successfully for both frontend and backend.
